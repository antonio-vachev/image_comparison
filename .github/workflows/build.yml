name: build

on:
  push:
    branches: [main]
    paths:
      - src/**

jobs:
  build-and-deploy:
    runs-on: windows-latest
    steps:
      - uses: actions/checkout@v3
      - uses: actions/setup-python@v4   
        with:
          python-version:   
            '3.10' 
      - run: pip install -r requirements.txt
      - run: |
          cd ./builds
          ./generate_new_build.py
      
